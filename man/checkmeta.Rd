% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_meta.R
\name{check_meta}
\alias{check_meta}
\alias{check_meta_test}
\alias{check_meta_undocumented_test}
\alias{check_meta_rd}
\alias{check_meta_unique_ids}
\title{Check Metadata Documentation}
\usage{
check_meta(
  files_rd = list.files("man", pattern = "*.Rd", full.names = TRUE),
  files_test = list.files("tests/testthat", pattern = "^test-.*\\\\.R$", full.names =
    TRUE),
  check_rd_tag_exist = c("Id", "Compliance Risk", "Implementation Complexity",
    "Test Scope"),
  check_test_tag_exist = c("author", "reviewer", "review_date", "description"),
  check_rd_tag_unique = "Id",
  exclude_package_rd = TRUE,
  concise = TRUE,
  print_error = FALSE
)

check_meta_test(
  files_test = list.files("tests/testthat", pattern = "^test-.*\\\\.R$", full.names =
    TRUE),
  check_test_tag_exist = c("author", "reviewer", "review_date", "description"),
  concise = FALSE,
  print_error = FALSE
)

check_meta_undocumented_test(
  files_test = list.files("tests/testthat", pattern = "^test-.*\\\\.R$", full.names =
    TRUE),
  concise = FALSE,
  print_error = FALSE
)

check_meta_rd(
  files_rd = list.files("man", pattern = "*.Rd", full.names = TRUE),
  check_rd_tag_exist = c("Id", "Compliance Risk", "Implementation Complexity",
    "Test Scope"),
  exclude_package_rd = TRUE,
  concise = FALSE,
  print_error = FALSE
)

check_meta_unique_ids(
  files_rd = list.files("man", pattern = "*.Rd", full.names = TRUE),
  check_rd_tag_unique = "Id",
  exclude_package_rd = TRUE,
  concise = FALSE,
  print_error = FALSE
)
}
\arguments{
\item{files_rd}{character; A vector of paths to \code{.Rd} files to be checked.}

\item{files_test}{character; A vector of paths to test files to be checked.}

\item{check_rd_tag_exist}{character; A vector of metadata tag names in Rd files to check
for completeness.}

\item{check_test_tag_exist}{character; A vector of metadata tag names in test files to check
for completeness.}

\item{check_rd_tag_unique}{character; A single metadata tag in Rd files to check for uniqueness.
Default is \code{"Id"}.}

\item{exclude_package_rd}{logical; Whether to exclude package-level Rd files (i.e., files ending
with \code{"-package.Rd"}). Default is \code{TRUE}.}

\item{concise}{logical; Whether to print only failures instead of detailed output. Default is
\code{TRUE}.}

\item{print_error}{logical; Whether to print error messages. This is useful for CI/CD pipelines
to produce errors when issues are found. Default is \code{FALSE}.}
}
\value{
\itemize{
\item \code{check_meta()}: A named list with results from each sub-check
(invisibly).
}
}
\description{
A suite of functions to validate metadata documentation in Rd and test files.

Meta tags follow the format \verb{@meta key value} where \code{key} can be any
user-defined identifier. Built-in tags for function documentation include
\code{id}, \code{compliance_risk}, and \code{implementation_complexity}. For test files,
the default required tags are \code{author}, \code{reviewer}, \code{review_date}, and
\code{description}. Custom tags can be added and validated by passing them to
the \code{check_rd_tag_exist} or \code{check_test_tag_exist} parameters.

This function combines multiple checks to ensure metadata in your Rd and test files meets
required standards for completeness, uniqueness, and documentation quality. The following
checks are included:
\itemize{
\item \code{\link[=check_meta_rd]{check_meta_rd()}}: Checks for missing metadata tags in Rd files.
\item \code{\link[=check_meta_unique_ids]{check_meta_unique_ids()}}: Ensures the 'Id' metadata tag in Rd files is unique.
\item \code{\link[=check_meta_undocumented_test]{check_meta_undocumented_test()}}: Validates that all \code{test_that} test cases
are properly documented.
\item \code{\link[=check_meta_test]{check_meta_test()}}: Verifies that test case documentation contains all required
metadata tags.
}
}
\meta{
Id: URS01.FS005

Compliance Risk: low

Implementation Complexity: low

Test Scope: low
}
