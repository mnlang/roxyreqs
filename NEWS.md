# roxyreqs 1.3.0

* First public release on GitHub and CRAN.
* `@meta` tags for test cases and function documentation.
* `JunitReporterMeta` exports test metadata to JUnit XML.
* `check_meta*()` functions validate required metadata in tests and
  `.Rd` files.
